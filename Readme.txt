 Clean the project
 Run mvn clean install to build the project
 Create a DB schema in the localhost greetings
 Use CreateDB.sql file to create db schema and tables
 Every Incoming and outgoing request should be in the encrypted format